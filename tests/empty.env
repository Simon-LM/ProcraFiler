# Intentionally empty. Pointed to by PROCRAFILER_ENV_FILE in tests/__init__.py
# so the test suite never loads the developer's real .env (no Mistral key, no
# chains) and stays offline + deterministic. Do not add variables here.
