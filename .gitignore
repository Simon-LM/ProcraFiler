# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/

# Virtual environments
.venv/
venv/

# IDE/OS
.vscode/
.idea/
.DS_Store
.directory

# Secrets (never commit runtime env files or keys)
*.env
!.env.example
*.key
*.pem

# User context file (personal data — only the .example template is committed)
context.txt
context.md

# ProcraFiler runtime artifacts
runtime/
logs/
history/

# Local dev sandbox for manual / end-to-end testing (real files, generated
# library/mirror/state, never committed)
sandbox/
